location / {
    # Block Specific IP
    # deny    192.168.1.1;

    # Allow INTERNAL IPs
    # allow   127.0.0.1;
    allow   10.0.0.0/8;
    allow   172.16.0.0/12;
    allow   192.168.0.0/16;

    # DROP ALL OTHER TRAFFIC
    deny    all;
}